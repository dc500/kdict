.row
  .span5.column
    - if ((typeof(result.korean.hangul) == '') || (result.korean.hangul == ''))
      h2
        a(href="/entries/#{result.id}") (Undefined)
    -else
      h2 
        a(href="/entries/#{result.id}") #{result.korean.hangul}

    h4= result.hanja

    .edit
      - if (typeof currentUser !== 'undefined')
        p
          a(href="/entries/#{result.id}/edit") Edit
      - else
        p
          small (Login to edit)


  .span11.column
    h2 Definitions
    //- if (results.definitions.length == 0)
    - if (typeof(results.definitions) != 'undefined')
      p No definition,
        a(href="/entries/#{result.id}/edit") add one!
    - else
      - each defs, lang in result.definitions
       ul
         - each definition, i in defs
           li= definition
