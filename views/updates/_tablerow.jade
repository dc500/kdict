include ../mixins
tr(class="#{tablerow.type}")
  td
    - if (tablerow.type == 'delete')
      a(href="/updates/#{tablerow.id}")= tablerow.before.korean.hangul
    - else
      a(href="/entries/#{tablerow.entry}")= tablerow.entry.korean
  td 
    mixin username_with_score(tablerow.user)
